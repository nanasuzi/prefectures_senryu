<div class="container">
  <div class="row">
    <div class="col-9 mx-auto">

<h1 class="text-center">都道府県一覧</h1>
  <ul class="d-flex flex-wrap flex-column list-unstyled" style="height:200px;">
    <% @themes.each do |theme| %>
      <li>
        <h5><%= link_to "#{theme.name}", theme_path(theme) %></h5>
      </li>
    <% end %>
  </ul>

    </div>
  </div>
</div>